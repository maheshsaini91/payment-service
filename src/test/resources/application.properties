# Disable Kafka auto-configuration in unit/slice tests.
# Integration tests that need a real broker should use @EmbeddedKafka instead.
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

ledger.base-url=http://localhost:8081
kafka.topic.payment-events=payment.events
